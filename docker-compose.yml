version: "3.9"

services:
  kolla-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kolla-backend
    ports:
      - "5007:5007"
    environment:
      - ASPNETCORE_URLS=http://+:5007